(()=>{'use strict';
const D=window.COURSE_DATA||{chapters:[],slides:[],knowledge:[],questionBank:[]};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const APP_KEY='td-study-os-100', COVER_KEY='td-slide-coverage-v1';
function read(k){try{return JSON.parse(localStorage.getItem(k)||'{}')}catch(e){return {}}}
function appState(){return read(APP_KEY)}function coverState(){return read(COVER_KEY)}
function daySlides(d){return D.slides.filter(s=>s.day===d)}
function slideDone(s){return !!coverState().pages?.[`d${s.day}-s${String(s.slide).padStart(3,'0')}`]}
function directK(d){return new Set(daySlides(d).flatMap(s=>s.knowledgeIds||[])).size}
function pendingFormal(d){return daySlides(d).filter(s=>String(s.auditStatus||'').includes('正式考點')&&!(s.knowledgeIds||[]).length).length}
function homeCoverage(){
 if(!location.hash.startsWith('#/home')&&location.hash!=='')return; const app=$('#app');if(!app)return;
 $('#coverageHomePanel')?.remove();$('#coverageHeroV2')?.remove();
 const ss=daySlides(1),done=ss.filter(slideDone).length,pct=ss.length?Math.round(done/ss.length*100):0,ks=D.knowledge.filter(k=>k.day===1).length,linked=directK(1),gap=pendingFormal(1);
 const box=document.createElement('section');box.id='coverageHeroV2';box.className='coverage-home-v2';box.innerHTML=`<div class="coverage-home-copy"><div class="eyebrow">COMPLETE MATERIAL COVERAGE</div><h2>Day1 不是少數重點，而是 ${ss.length} 張教材頁＋${ks} 個 Knowledge</h2><p>完整學習先逐張確認教材，再把可考內容整合成 Knowledge；上班快卡與題庫則從同一批 Knowledge 做主動回想。</p><div class="coverage-home-actions"><a class="btn primary" href="#/slides">開始逐頁完整複習</a><a class="btn" href="#/course">切到考點深度學習</a></div></div><div class="coverage-home-score"><strong>${done}/${ss.length}</strong><span>Day1 已逐頁確認</span><div class="coverage-progress"><i style="width:${pct}%"></i></div><small>${pct}% Slide Coverage</small></div><div class="coverage-home-facts"><span><b>${ks}</b> Day1 Knowledge</span><span><b>${linked}</b> 已被頁面直接連結</span><span class="${gap?'warn-text':''}"><b>${gap}</b> 正式考點頁待補建</span></div>`;
 const head=app.querySelector('.page-head'); if(head)head.insertAdjacentElement('afterend',box); else app.prepend(box);
}
function lastScoreMap(){const st=appState(),map={};for(const a of st.attempts||[])if(a?.knowledgeId&&a.score!=null)map[a.knowledgeId]=+a.score;return map}
function typeStats(){const scores=lastScoreMap(),types=['定義','架構','流程','方法','條項'],out={};for(const t of types){const ks=D.knowledge.filter(k=>k.type===t||(k.examTags||[]).includes(t)),tested=ks.filter(k=>scores[k.id]!=null),avg=tested.length?tested.reduce((n,k)=>n+scores[k.id],0)/tested.length:0,cover=ks.length?tested.length/ks.length:0;out[t]={count:ks.length,tested:tested.length,avg:Math.round(avg),cover:Math.round(cover*100),effective:Math.round(avg*cover)}}return out}
function drawRadar(canvas,vals,labels){if(!canvas)return;const ctx=canvas.getContext('2d'),w=canvas.width,h=canvas.height,cx=w/2,cy=h/2+8,R=Math.min(w,h)*.35,N=labels.length;ctx.clearRect(0,0,w,h);ctx.font='12px sans-serif';ctx.textAlign='center';ctx.textBaseline='middle';for(let ring=1;ring<=4;ring++){ctx.beginPath();for(let i=0;i<N;i++){const a=-Math.PI/2+2*Math.PI*i/N,r=R*ring/4,x=cx+r*Math.cos(a),y=cy+r*Math.sin(a);i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();ctx.strokeStyle='#dbe3e7';ctx.stroke()}for(let i=0;i<N;i++){const a=-Math.PI/2+2*Math.PI*i/N,x=cx+R*Math.cos(a),y=cy+R*Math.sin(a);ctx.beginPath();ctx.moveTo(cx,cy);ctx.lineTo(x,y);ctx.strokeStyle='#e5eaed';ctx.stroke();const lx=cx+(R+32)*Math.cos(a),ly=cy+(R+25)*Math.sin(a);ctx.fillStyle='#43515e';ctx.fillText(`${labels[i]} ${vals[i]}`,lx,ly)}ctx.beginPath();vals.forEach((v,i)=>{const a=-Math.PI/2+2*Math.PI*i/N,r=R*Math.max(0,Math.min(100,v))/100,x=cx+r*Math.cos(a),y=cy+r*Math.sin(a);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.closePath();ctx.fillStyle='rgba(23,63,58,.16)';ctx.fill();ctx.strokeStyle='#173f3a';ctx.lineWidth=2;ctx.stroke()}
function heatmap(tsScores){const types=['定義','架構','流程','方法','條項'];let h='<table class="heat-table"><thead><tr><th>Day</th>'+types.map(t=>`<th>${t}</th>`).join('')+'</tr></thead><tbody>';for(let d=1;d<=5;d++){h+=`<tr><th>Day ${d}</th>`;for(const t of types){const ks=D.knowledge.filter(k=>k.day===d&&(k.type===t||(k.examTags||[]).includes(t))),a=ks.map(k=>tsScores[k.id]).filter(x=>x!=null),avg=a.length?Math.round(a.reduce((s,x)=>s+x,0)/a.length):0,cover=ks.length?a.length/ks.length:0,eff=Math.round(avg*cover),cl=eff>=70?'heat-3':eff>=40?'heat-2':'heat-1';h+=`<td class="${cl}">${a.length?eff:'—'}</td>`}h+='</tr>'}return h+'</tbody></table>'}
function chapterTable(scores){return `<table class="analysis-table"><thead><tr><th>章節</th><th>考點</th><th>已測</th><th>平均</th><th>有效分</th></tr></thead><tbody>${D.chapters.map(c=>{const ks=D.knowledge.filter(k=>k.chapter===c.id),a=ks.map(k=>scores[k.id]).filter(x=>x!=null),avg=a.length?Math.round(a.reduce((s,x)=>s+x,0)/a.length):0,cover=ks.length?a.length/ks.length:0,eff=Math.round(avg*cover);return `<tr><td style="text-align:left"><b>${esc(c.id.replace('_','-'))}</b> ${esc(c.title)}</td><td>${ks.length}</td><td>${a.length}</td><td>${a.length?avg+'%':'—'}</td><td><b>${a.length?eff:'—'}</b></td></tr>`}).join('')}</tbody></table>`}
function analyticsPlus(){
 if(!location.hash.startsWith('#/analytics'))return;const app=$('#app');if(!app)return;$('#analyticsPlusV2')?.remove();
 const scores=lastScoreMap(),ts=typeStats();const box=document.createElement('section');box.id='analyticsPlusV2';box.innerHTML=`<div class="analytics-grid analytics-plus"><section class="panel"><h2>五大核心能力雷達</h2><div class="radar-wrap"><canvas id="radarV2" width="520" height="360"></canvas></div><p class="tiny muted">有效分＝最近作答平均 × 已測覆蓋率。只做少數熟題，不會得到虛高分數。</p></section><section class="panel"><h2>能力分析表</h2><table class="analysis-table"><thead><tr><th>能力</th><th>作答平均</th><th>覆蓋率</th><th>有效分</th></tr></thead><tbody>${Object.entries(ts).map(([t,x])=>`<tr><td><b>${t}</b></td><td>${x.avg}%</td><td>${x.tested}/${x.count} (${x.cover}%)</td><td><b>${x.effective}</b></td></tr>`).join('')}</tbody></table></section></div><div class="analytics-grid analytics-plus"><section class="panel"><h2>Day × 類型熱圖</h2>${heatmap(scores)}</section><section class="panel"><h2>逐頁 Coverage</h2><div class="coverage-day-table">${[1,2,3,4,5].map(d=>{const ss=daySlides(d),done=ss.filter(slideDone).length,p=ss.length?Math.round(done/ss.length*100):0;return `<div><span>Day ${d}</span><div class="coverage-progress"><i style="width:${p}%"></i></div><b>${done}/${ss.length}｜${p}%</b></div>`}).join('')}</div></section></div><section class="panel analytics-plus"><h2>逐章掌握度</h2>${chapterTable(scores)}</section>`;
 const metrics=app.querySelector('.grid.four');if(metrics)metrics.insertAdjacentElement('afterend',box);else app.appendChild(box);drawRadar($('#radarV2'),Object.values(ts).map(x=>x.effective),Object.keys(ts));
}
function drivePreview(){
 if(!location.hash.startsWith('#/slide/'))return;const m=location.hash.match(/^#\/slide\/(\d+)\/(\d+)/);if(!m)return;const day=+m[1],slide=+m[2],s=D.slides.find(x=>x.day===day&&x.slide===slide);if(!s)return;const card=$('.evidence-card');if(!card||card.querySelector('.drive-preview-wrap'))return;const url=D.sources?.[day]?.url||'';const id=(url.match(/\/d\/([^/]+)/)||[])[1];if(!id)return;const wrap=document.createElement('div');wrap.className='drive-preview-wrap';wrap.innerHTML=`<div class="drive-preview-head"><b>教材原頁內嵌預覽</b><span>目標：PDF P${s.pdfPage}${s.position?'｜'+esc(s.position):''}</span></div><iframe class="drive-preview" loading="lazy" referrerpolicy="no-referrer" src="https://drive.google.com/file/d/${esc(id)}/preview#page=${s.pdfPage}" title="Day ${day} PDF P${s.pdfPage}"></iframe><p class="tiny muted">Google Drive 若未自動跳到指定頁，請依上方 PDF P${s.pdfPage} 對照；原始投影片不複製進 Public GitHub。</p>`;card.appendChild(wrap);
}
function slideStatus(){
 if(!location.hash.startsWith('#/slide/'))return;const m=location.hash.match(/^#\/slide\/(\d+)\/(\d+)/);if(!m)return;const s=D.slides.find(x=>x.day===+m[1]&&x.slide===+m[2]);if(!s)return;const host=$('.slide-detail-main');if(!host||$('#pageTeachingStatus'))return;const g=s.guide||{},box=document.createElement('section');box.id='pageTeachingStatus';box.className='coverage-teach-card teaching-status-card';const k=(s.knowledgeIds||[]).length;box.innerHTML=`<h2>本頁教學化狀態</h2><div class="tags"><span class="tag strong">${esc(g.teachingStatus||'Coverage已建立')}</span><span class="tag">${k} 個直接 Knowledge</span><span class="tag exam">${esc(g.pageRole||s.auditStatus||'')}</span></div><p>${esc(g.pagePrompt||'先看原頁，再用自己的話說出這頁在本章的作用。')}</p>`;host.insertAdjacentElement('afterbegin',box)
}
function routeRepair(){const h=location.hash||'#/home';setTimeout(()=>{if(h.startsWith('#/home')||h==='#/'||h==='#')homeCoverage();if(h.startsWith('#/analytics'))analyticsPlus();if(h.startsWith('#/slide/')){slideStatus();drivePreview()}},80)}
window.addEventListener('hashchange',routeRepair);window.addEventListener('DOMContentLoaded',routeRepair);setTimeout(routeRepair,150);
})();